---
profile: basic

exclude_paths:
  - group_vars/all/vault.yml
  - tests/vars.yml

# These rules require switching all tasks to FQCN (e.g. ansible.builtin.copy)
# and normalising boolean values — enable incrementally when ready.
warn_list:
  - fqcn[action-core]
  - yaml[truthy]
